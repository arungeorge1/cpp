#include <iostream>
using namespace std;
int fact(int n)
{
    int i,factorial=1;
    for(i=2;i<=n;i++)
    {
        factorial*=i;
    }
    return factorial;
}
int main() 
{
    int n,ans;
    cout<<"Enter a number: ";
    cin>>n;
    ans=fact(n);
    cout<<ans;
    return 0;
}
